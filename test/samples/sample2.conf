test-duplicates {
            api {
                ip = 98
                ip = ${?IP_ENV_VARIABLE}
            }
            testing {
                ip = 98
            }
            web {
                port = ${?PORT_ENV_VARIABLE}  
                port = 8080
            }
            key {
                name = ${?NAME_ENV_VARIABLE}   
            }
            configuration {
                description = "Test"
            }
}
